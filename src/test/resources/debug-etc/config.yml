#
# Module configuration file
#
# Please, modify the configuration below to match your environment.
# Also, delete and/or modify this description.

logging:
  level: INFO
  appenders:
    - type: console
    - type: file
      archive: false
      currentLogFilename: data/dd-vault-metadata.log
  loggers:
    'nl.knaw.dans': DEBUG
    'org.hibernate.engine.internal.StatisticalLoggingSessionEventListener': 'OFF'
